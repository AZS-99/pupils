from Stack import Stack
from Queue import Queue
from recursion import sigma
from recursion import factorial
from revision import population
from revision import c_to_f
from revision import a
from revision import perimeter
from revision import better_deal
from revision import triangle_area
from revision import linear_equation
from revision import divisibility
from revision

if __name__ == '__main__':


    # edge1, edge2, edge3 = input("Enter three edges:").split(', ')
    #
    # edge1 = int(edge1)
    # edge2 = int(edge2)
    # edge3 = int(edge3)
    #
    # print("The perimeter is", perimeter(edge1, edge2, edge3))
    #
    # weight, price = input("Enter weight and price for Package 1:")

    # x1, y1, x2, y2, x3, y3 = input("Enter three points of a triangle: ").split(', ')
    # x1 = float(x1)
    # x2 = float(x2)
    # x3 = float(x3)
    # y1 = float(y1)
    # y2 = float(y2)
    # y3 = float(y3)
    # print("The area is", triangle_area(x1, y1, x2, y2, x3, y3))

    # a, b, c, d, e, f = input("Enter a, b, c, d, e, f: ").split(', ')
    # a = float(a)
    # b = float(b)
    # c = float(c)
    # d = float(d)
    # e = float(e)
    # f = float(f)
    #
    # x, y = linear_equation(a, b, c, d, e, f)
    # print("x is", x, "and y is", y)

    number = int(input("Enter any integer: "))
    divisibility(number)









